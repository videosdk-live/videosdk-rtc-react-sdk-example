export const activeSpeakerBasedMainTileAlgoConfig = {
  SAMPLING_INTERVAL: 50, // Sample active speaker every 50ms
  ANALYZER_INTERVAL: 300, // Analyze window every 300ms
  UPDATE_INTERVAL: 600, // UI update every 600ms
  DECAY_RATE: 0.95, // Decay rate
  BASE_SCORE: 0.5, // Base score
};
